import { Pressable,Text,View } from 'react-native'; import type { ReactNode } from 'react'; import { router } from 'expo-router'; import { Avatar } from './Avatar';
export function UserCard({user,action}:{user:any;action?:ReactNode}){return <Pressable onPress={()=>router.push({pathname:'/profile/[id]',params:{id:user.id}})} style={{padding:14,flexDirection:'row',alignItems:'center',gap:12}}><Avatar uri={user.avatar_path} name={user.full_name}/><View style={{flex:1}}><Text style={{fontWeight:'900'}}>{user.full_name||user.username}</Text><Text style={{color:'#6B7280'}}>@{user.username}</Text></View>{action}</Pressable>}
