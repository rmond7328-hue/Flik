import { useEffect,useState } from 'react'; import { Stack,router,useSegments } from 'expo-router'; import { ActivityIndicator,View } from 'react-native'; import { StatusBar } from 'expo-status-bar'; import { supabase } from '../lib/supabase'; import { useAuthStore } from '../stores/auth-store';
export default function RootLayout(){const{initialized,session,setAuth,setInitialized}=useAuthStore();const segments=useSegments();const[profileReady,setProfileReady]=useState(false);const[needsOnboarding,setNeedsOnboarding]=useState(false);useEffect(()=>{let mounted=true;supabase.auth.getSession().then(async({data})=>{if(!mounted)return;setAuth(data.session);if(data.session){const p=await supabase.from('profiles').select('full_name,username,campus_id').eq('id',data.session.user.id).maybeSingle();if(mounted)setNeedsOnboarding(!p.data?.full_name||!p.data?.username||!p.data?.campus_id); }else if(mounted)setNeedsOnboarding(false);if(mounted){setProfileReady(true);setInitialized(true)}});const{data:listener}=supabase.auth.onAuthStateChange((_e,next)=>{setAuth(next);if(!next){setNeedsOnboarding(false);setProfileReady(true)}else{setProfileReady(false);supabase.from('profiles').select('full_name,username,campus_id').eq('id',next.user.id).maybeSingle().then(({data})=>{if(mounted){setNeedsOnboarding(!data?.full_name||!data?.username||!data?.campus_id);setProfileReady(true)}})}});return()=>{mounted=false;listener.subscription.unsubscribe()}},[setAuth,setInitialized]);useEffect(()=>{if(!initialized||!profileReady)return;const first=segments[0] as string|undefined;const inAuth=first==='(auth)';const inOnboarding=first==='(onboarding)';const inTabs=first==='(tabs)';if(!session&&!inAuth)router.replace('/(auth)/login');else if(session&&needsOnboarding&&!inOnboarding)router.replace('/(onboarding)/profile');else if(session&&!needsOnboarding&&(inAuth||inOnboarding||!inTabs))router.replace('/(tabs)/home')},[initialized,profileReady,session,needsOnboarding,segments]);if(!initialized||!profileReady)return <View style={{flex:1,alignItems:'center',justifyContent:'center',backgroundColor:'#fff'}}><ActivityIndicator size="large" color="#60A5FA"/></View>;return <><StatusBar style="dark"/><Stack screenOptions={{headerShown:false}}/></>}
