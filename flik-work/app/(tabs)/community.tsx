import { useEffect,useState } from 'react'; import { Alert,FlatList,Modal,Pressable,Text,TextInput,View } from 'react-native'; import { router } from 'expo-router'; import { useAuthStore } from '../../stores/auth-store'; import { listCommunities,createCommunity } from '../../services/feed'; import { supabase } from '../../lib/supabase'; import { CommunityCard } from '../../components/CommunityCard'; import { Skeleton } from '../../components/Skeleton'; import { EmptyState } from '../../components/EmptyState';
export default function Community(){const user=useAuthStore(s=>s.user);const[items,setItems]=useState<any[]>([]);const[campus,setCampus]=useState<string>();const[loading,setLoading]=useState(true);const[open,setOpen]=useState(false);const[name,setName]=useState('');const[desc,setDesc]=useState('');const[rules,setRules]=useState('');const[privateC,setPrivateC]=useState(false);const[creating,setCreating]=useState(false);async function load(){if(!user)return;const p=await supabase.from('profiles').select('campus_id').eq('id',user.id).single();setCampus(p.data?.campus_id);if(p.data?.campus_id){const r=await listCommunities(p.data.campus_id);setItems(r.data||[])}setLoading(false)}useEffect(()=>{load()},[user]);async function create(){if(!user||!campus||name.trim().length<3)return;setCreating(true);const slug=name.trim().toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');const r=await createCommunity({name:name.trim(),slug,description:desc.trim(),rules:rules.trim(),campus_id:campus,created_by:user.id,visibility:privateC?'private':'public'});setCreating(false);if(r.error)return Alert.alert('Could not create community',r.error.message);setOpen(false);setName('');setDesc('');setRules('');setPrivateC(false);load();router.push({pathname:'/community/[id]',params:{id:r.data.id}})}return <View style={{flex:1,backgroundColor:'#fff',paddingTop:58}}><View style={{paddingHorizontal:16,paddingBottom:14,flexDirection:'row',alignItems:'center'}}><Text style={{fontSize:30,fontWeight:'900',flex:1}}>Community</Text><Pressable onPress={()=>setOpen(true)} style={{backgroundColor:'#60A5FA',paddingHorizontal:14,paddingVertical:10,borderRadius:12}}><Text style={{color:'#fff',fontWeight:'900'}}>Create</Text></Pressable></View>{loading?<View style={{padding:16,gap:12}}>{[1,2,3].map(x=><Skeleton key={x} height={120} radius={16}/>)}</View>:<FlatList contentContainerStyle={{padding:16}} data={items} keyExtractor={x=>x.id} renderItem={({item})=><CommunityCard community={item}/>} ListEmptyComponent={<EmptyState title="Find your people" message="Create the first community for your campus." actionLabel="Create community" onAction={()=>setOpen(true)}/>}/>}<Modal visible={open} transparent animationType="slide" onRequestClose={()=>setOpen(false)}><View style={{flex:1,justifyContent:'flex-end',backgroundColor:'rgba(0,0,0,.25)'}}><View style={{backgroundColor:'#fff',padding:20,borderTopLeftRadius:24,borderTopRightRadius:24,gap:10}}><Text style={{fontSize:24,fontWeight:'900'}}>New community</Text><TextInput placeholder="Community name" value={name} onChangeText={setName} style={input}/><TextInput placeholder="Description" value={desc} onChangeText={setDesc} multiline style={[input,{minHeight:80}]}/><TextInput placeholder="Community rules" value={rules} onChangeText={setRules} multiline style={[input,{minHeight:80}]}/><Pressable onPress={()=>setPrivateC(!privateC)} style={{padding:14,backgroundColor:'#F5F5F5',borderRadius:12}}><Text style={{fontWeight:'800'}}>{privateC?'Private community':'Public community'} · tap to change</Text></Pressable><View style={{flexDirection:'row',gap:10}}><Pressable onPress={()=>setOpen(false)} style={[button,{backgroundColor:'#F5F5F5'}]}><Text style={{fontWeight:'900'}}>Cancel</Text></Pressable><Pressable onPress={create} disabled={creating} style={button}><Text style={{color:'#fff',fontWeight:'900'}}>{creating?'Creating…':'Create'}</Text></Pressable></View></View></View></Modal></View>}
const input={height:48,borderWidth:1,borderColor:'#E5E7EB',borderRadius:13,paddingHorizontal:14};const button={flex:1,height:48,borderRadius:13,backgroundColor:'#60A5FA',alignItems:'center' as const,justifyContent:'center' as const};
