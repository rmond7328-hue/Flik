import { Pressable, Text, View } from 'react-native';
export function TopTabs({value,onChange}:{value:string;onChange:(v:string)=>void}){return <View style={{flexDirection:'row',backgroundColor:'#F5F5F5',borderRadius:14,padding:4}}>{['feed','following','discover'].map(x=><Pressable key={x} onPress={()=>onChange(x)} style={{flex:1,height:40,borderRadius:11,alignItems:'center',justifyContent:'center',backgroundColor:value===x?'#fff':'transparent'}}><Text style={{fontWeight:value===x?'900':'700',color:value===x?'#111':'#6B7280'}}>{x[0].toUpperCase()+x.slice(1)}</Text></Pressable>)}</View>}
